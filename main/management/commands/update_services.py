"""
Команда для обновления описаний услуг
Запускать: python manage.py update_services
"""

from django.core.management.base import BaseCommand
from main.models import Service

class Command(BaseCommand):
    help = 'Обновляет описания услуг'

    def handle(self, *args, **options):
        self.stdout.write("Обновляем описания услуг...")
        
        # Обновляем описания услуг
        services_data = {
            '3d_modeling': 'Создаём модели под разные задачи. Параметрическое моделирование подходит для инженерии и точных расчётов, а полигональное — для дизайна, фигурок, визуализации и т. п.. Такой подход помогает решать как технические, так и креативные задачи.',
            '3d_printing': 'Изготавливаем изделия с применением FDM, SLA, SLS, SLM и других технологий. FDM подходит для прототипов, SLA обеспечивает высокую детализацию, а SLS и SLM позволяют печатать прочные детали. Мы подбираем метод под цели вашего проекта.',
            '3d_scanning': 'Оцифровываем объекты любой формы и сложности. Технология используется для анализа геометрии, архивации и подготовки моделей к редактированию или печати. Это удобный способ перенести физический объект в цифровой формат.',
            'reverse_engineering': 'Создаём точные 3D-модели по готовым объектам. Эта услуга нужна для восстановления редких деталей, модернизации изделий и адаптации их под новые задачи. Она позволяет быстро находить рабочие решения.',
            'engineering': 'Мы превращаем идеи в реальные проекты. Инжиниринг помогает разрабатывать новые изделия, улучшать конструкции и создавать прототипы, готовые к производству. Такой подход экономит время и снижает риски ошибок.',
            'post_processing': 'Доводим изделия до финального вида. Шлифовка, окраска, полировка и другие доработки улучшают внешний вид и свойства, делая продукцию профессиональной и готовой к применению.'
        }
        
        updated_count = 0
        for service_type, description in services_data.items():
            try:
                service = Service.objects.get(service_type=service_type)
                service.description = description
                service.save()
                self.stdout.write(f"✅ Обновлено: {service.get_service_type_display()}")
                updated_count += 1
            except Service.DoesNotExist:
                self.stdout.write(f"❌ Услуга {service_type} не найдена")
        
        self.stdout.write(f"\n🎉 Обновлено {updated_count} услуг!")

Команда для обновления описаний услуг
Запускать: python manage.py update_services
"""

from django.core.management.base import BaseCommand
from main.models import Service

class Command(BaseCommand):
    help = 'Обновляет описания услуг'

    def handle(self, *args, **options):
        self.stdout.write("Обновляем описания услуг...")
        
        # Обновляем описания услуг
        services_data = {
            '3d_modeling': 'Создаём модели под разные задачи. Параметрическое моделирование подходит для инженерии и точных расчётов, а полигональное — для дизайна, фигурок, визуализации и т. п.. Такой подход помогает решать как технические, так и креативные задачи.',
            '3d_printing': 'Изготавливаем изделия с применением FDM, SLA, SLS, SLM и других технологий. FDM подходит для прототипов, SLA обеспечивает высокую детализацию, а SLS и SLM позволяют печатать прочные детали. Мы подбираем метод под цели вашего проекта.',
            '3d_scanning': 'Оцифровываем объекты любой формы и сложности. Технология используется для анализа геометрии, архивации и подготовки моделей к редактированию или печати. Это удобный способ перенести физический объект в цифровой формат.',
            'reverse_engineering': 'Создаём точные 3D-модели по готовым объектам. Эта услуга нужна для восстановления редких деталей, модернизации изделий и адаптации их под новые задачи. Она позволяет быстро находить рабочие решения.',
            'engineering': 'Мы превращаем идеи в реальные проекты. Инжиниринг помогает разрабатывать новые изделия, улучшать конструкции и создавать прототипы, готовые к производству. Такой подход экономит время и снижает риски ошибок.',
            'post_processing': 'Доводим изделия до финального вида. Шлифовка, окраска, полировка и другие доработки улучшают внешний вид и свойства, делая продукцию профессиональной и готовой к применению.'
        }
        
        updated_count = 0
        for service_type, description in services_data.items():
            try:
                service = Service.objects.get(service_type=service_type)
                service.description = description
                service.save()
                self.stdout.write(f"✅ Обновлено: {service.get_service_type_display()}")
                updated_count += 1
            except Service.DoesNotExist:
                self.stdout.write(f"❌ Услуга {service_type} не найдена")
        
        self.stdout.write(f"\n🎉 Обновлено {updated_count} услуг!")









