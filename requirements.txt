# ИСПРАВЛЕННЫЕ зависимости для продакшена
# Все версии синхронизированы и обновлены

Django==3.2.23
Pillow==10.0.0
gunicorn==21.2.0
psycopg2-binary==2.9.7
whitenoise==6.5.0

# Дополнительные зависимости для продакшена
python-decouple==3.8  # Для управления переменными окружения
django-environ==0.10.0  # Альтернатива для переменных окружения
gunicorn[gevent]==21.2.0  # Для лучшей производительности
supervisor==4.2.5  # Для управления процессами
